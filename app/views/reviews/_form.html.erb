<!--<h1>Review</h1>-->

<!--<p>-->
<!--  <strong>Name:</strong>-->
  <%#= @review.name %>
<!--</p>-->

<!--<p>-->
<!--  <strong>Body:</strong>-->
  <%#= @review.body %>
<!--</p>-->
<%#= link_to 'Delete Review', review_path(@review), method: :delete, data: { confirm: 'Are you sure?' } %>

<!-- app/views/reviews/_form.html.erb -->
<%= form_with(model: review, local: true) do |form| %>
  <% if review.errors.any? %>
    <div id="error_explanation">
      <h2><%= t('errors.title', count: review.errors.count) %> <%= t('errors.messages.prohibited') %></h2> // TODO:


      <ul>
        <% review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <% if user_signed_in? %>
    <%= form.hidden_field :name, value: current_user.username %>
    <div class="form-group">
      <%= form.label :name, t('reviews.form.your_name') %>
      <input type="text" value="<%= current_user.username %>" class="form-control" readonly/>
    </div>
  <% else %>
    <div class="form-group">
      <%= form.label :name, t('reviews.form.name') %>
      <%= form.text_field :name, class: 'form-control' %>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :body, t('reviews.form.body') %>
    <%= form.text_area :body, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :doctor_id, t('reviews.form.doctor') %>
    <%= form.collection_select :doctor_id, Doctor.all, :id, :name, include_blank: true, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit t('helpers.submit.create'), class: 'btn btn-primary' %>
  </div>
<% end %>

